# Working Memory — robert-klucovsky — 2026-07-16 (→ 2026-07-17)

Veľká session: bezpečnostné hodnotenie celého home-labu → plná remediácia (aplikovaná
na živé systémy) → incident RustFS po odstávke → segregácia NVIDIA DGX Spark (rozpracované).

## 2026-07-16 — Bezpečnostné hodnotenie (report)

Dodané: `docs/security/2026-07-14-home-lab-risk-assessment.md` (SK), spec+plán v
`docs/superpowers/specs|plans/2026-07-14-*`. Dva scenáre: bez WireGuardu / s WireGuardom.
Kľúčové overené fakty (Omada read-only API + SSH na cwwk):
- Perimeter ER605 (172.16.1.1): forward len **TCP 80/443 → 172.16.1.11**; WireGuard **site-to-site
  "RACK"** na ER605, **UDP 51820**, tunel **10.172.16.0/24**, 3 peery /32 (MacBook .2, Motorola .3,
  MacBookProM5 .4). `serverClientWireguard=false` (road-warrior cez site-to-site).
- Ploché VLAN: 1 (172.16.1.0/24), 10 OpenStack (172.16.2.0/24), 20 Ceph (172.16.3.0/24) —
  `isolation=false`, gateway ACL vypnuté. **OpenStack/Ceph cloud (9 serverov: 6× HP ProLiant DL3xx
  + 3 neznačkové, 10G+40G switch) je VYPNUTÝ** — VLAN 10/20 prázdne. Beží len cwwk + MAAS/Omada box.
- Node cwwk (172.16.1.11): **žiadny host firewall**; k8s API 6443 / kubelet 10250 / etcd 2379 /
  NFS 2049 / node_exporter 9100 / NodePorty (30432 Postgres plaintext, 30910/11 RustFS, 30417/18 Tempo)
  dosiahnuteľné z LAN/VPN. API/kubelet/etcd ale vynucujú auth (overené).
- **172.16.1.2 = kombinovaný MAAS + Omada box** (DNS 53, MAAS 5240/5443, squid 3128, Omada 8043).
  MAAS robí DHCP/DNS pre existujúce VLAN (router DHCP vypnutý).

## 2026-07-16 — Remediácia (APLIKOVANÁ, merged do main, pushed)

Vetva `security/remediation-2026-07-15` → fast-forward do `main` → push. Commity `08cb6b4..c53ae6a`.
- **A1** nová verejná gateway na **172.16.1.12** (`tf/gateway-public.tf`) — servíruje LEN
  s3/auth/fatto-aac, ostatné 404. **Router prehodený 80/443 .11→.12** (spravil operátor). Existujúca
  .11→relay→.13 cesta ostáva interná (LAN/VPN cez MAAS DNS → .11). Split-horizon DNS zachovaný.
- **A2** Keycloak `start-dev`→`start` (prod profil overený, OIDC 200). `tf/keycloak.tf`.
- **A3** Postgres **hostssl** vynútené na CNPG (`tf/cnpg.tf` pg_hba hostnossl reject); všetci konzumenti
  `sslmode=require` (provider `tf/main.tf`, Keycloak JDBC `tf/postgresql.tf`, README). Plaintext odmietaný (overené).
- **B2** Omada gateway ACL `deny-vpn-to-node-mgmt`: wg-tunnel(10.172.16.0/24) → k8s-mgmt-ports
  (172.16.1.11:2379/10250/9100) Deny. **B3** `deny-vpn-to-cloud`: wg-tunnel → 172.16.2.0/24+172.16.3.0/24.
- **C1** NFS `/data` export zúžený na peer /32 (10.172.16.2/3/4) + LAN, `secure` (bolo `insecure` + celé /24).
- **D** `dev/` → `tf/` rename (celý repo + README + .cortex refs). `terraform.tfvars` odsledovaný z gitu.
- Status: **open** — B2 peer-test (operátor z reálneho WG peera: 2379/10250/9100 blokované, ostatné OK).
Tags: #remediation #security #decided

## 2026-07-16 — KOREKCIA: žiadny reálny secret leak

Pôvodne som označil `terraform.tfvars` (tracked) ako "uniknuté heslá + DO token, rotovať + prepis histórie".
**Nesprávne.** Fingerprint analýza histórie: committed hodnoty boli **placeholdery** (postgres_superuser_password
aj digitalocean_token = ten istý 10-znakový dummy). **Reálne secrets žijú v gitignored `secrets.auto.tfvars`**
(*.auto.tfvars OVERRIDE-uje terraform.tfvars) a NIKDY neboli committnuté. Rotácia + history rewrite (D2/D3/D5)
= **zrušené, netreba.** Ponaučenie: over hodnoty (nie len existenciu kľúča) pred označením za leak.
Tags: #correction #security #ai-violation

## 2026-07-16 — RustFS incident po odstávke (vyriešené operátorom)

Po reštarte servera RustFS `/health/ready` 503 (alive, listening, ale nie ready). Root cause: startup scan
`read_all_data NotFound` + `list_path_raw VolumeNotFound` — nekonzistentný erasure store po nečistom vypnutí,
`.rustfs.sys` reinicializované; single-disk `xl-single`, **RustFS beta nemá heal command**. Dáta boli väčšinou
prítomné (aac-kpsdesign PDF ~896K), nie masívna strata. Riešené nakoniec operátorom ("už to beží"). Raw záloha
disk.img na node `/root/rustfs-backup-2026-07-16/data.tgz`.
Tags: #incident #rustfs #resolved

## 2026-07-17 — DGX Spark segregácia (ROZPRACOVANÉ)

Cieľ: DGX (gx10-988a, teraz 172.16.1.30 VLAN1, MAC A0-AD-9F-DC-98-8A; druhý DGX pribudne) **izolovať od
zvyšku siete, ale byť dosiahnuteľné z LAN**. Rozhodnutia: internet ÁNO / LAN NIE; oba DGX sa vidia (rovnaká VLAN);
**DHCP/DNS cez MAAS (DHCP-relay)**, DGX používajú verejné DNS, registrované v MAAS ako devices s rezervovanou IP.
Plán: VLAN 40 "dgx" 172.16.4.0/24, ER605 DHCP-relay → 172.16.1.2, ACL deny dgx→internal-lan (internet povolený,
LAN→DGX cez stateful returns). **BLOKER: neznámy switch/port DGX** (Omada hlási connectDevType=gateway) — čaká sa
na operátora kde je DGX fyzicky zapojený.
Status: open  Tags: #dgx #segmentation #network #open

## Operačné ponaučenia (session)

- **Omada API** (write funguje): base `https://172.16.1.2:8043/{omadacId}/api/v2`, login `POST /api/v2/login`
  (Csrf-Token header + cookie), omadacId `911e6d163a1e7e9e20ae3728e4ae7cc3`, site `67fe23d9da5d1c5ac5a98a71`.
  Endpointy: `setting/transmission/portForwardings`, `setting/firewall/acls?type=0` (gateway ACL; `aclDisable`
  je UI-hint, NIE global-off), `setting/profiles/groups` (IP/IP-Port groups). Schémy sa dajú vytiahnuť z frontend JS chunkov.
- **rawfile CSI** (openebs `csi-rawfile-default`) ukladá disk.img na host `/var/snap/k8s/common/rawfile-storage/pvc-*/`
  (v CSI kontajneri mount-nuté ako `/data` → chybové cesty vyzerajú ako `/data/pvc-*/disk.img`).
- **macOS `timeout` neexistuje** → `timeout ... /dev/tcp` dáva false negatívy; použi `nc -z -G`.
- Terminujúca Cilium gateway funguje aj priamo na svojej LB IP (overené na .13) — netreba passthrough+relay pre novú gateway.
