# Working Memory — robert-klucovsky — 2026-06-28

## 2026-06-28 21:03 — cortex init (tf-platform)

Inicializovaný project cortex `.cortex/` (canonical naming) pre tf-platform.
Seed obsah vyťažený z `README.md`, `dev/main.tf`, `dev/variables.tf`, `.terraform.lock.hcl`,
`docs/superpowers/`, git log.

Vytvorené wings: current-state, architectural-essence, platform-services, tenant-integration,
decisions-with-why, risks-and-watchouts, tribal-knowledge, people-dynamics (+ robert-klucovsky),
glossary-essence. Zaregistrované v user cortex `cross_project_allow_list`.

Kľúčové fakty zachytené: single root module v `dev/`, apply-prvý/destroy-posledný order,
pg backend (dočasne local pre teardown), project-agnostic OIDC (tenant-owned, role-based MinIO providers),
nexus timeout=60, cluster @ 172.16.1.11.

Status: decided
Tags: #cortex-init #mode-archive

## 2026-06-28 21:1x — CoreDNS hosts block via Terraform

Pridaný `dev/coredns.tf`: `kubernetes_config_map_v1_data.coredns_hosts` patchuje
`Corefile` kľúč v `ck-dns-coredns` ConfigMap (kube-system) a pridáva `auth.klucovsky.com`
do existujúcej hosts línie (`172.16.1.11 registry.klucovsky.com nexus.klucovsky.com auth.klucovsky.com`).

Zistené (source-first, kubectl):
- CoreDNS NEbol v Terraforme; ConfigMap je Helm-owned (release `ck-dns`, chart coredns-1.39.2,
  Canonical K8s built-in DNS feature). Hosts blok (registry+nexus) tam už existoval ~59 dní.
- Preto použitý `kubernetes_config_map_v1_data` + `force=true` (prevezme field ownership od Helm),
  nie full `kubernetes_config_map` (kolízia s existujúcim Helm-managed objektom).
- CoreDNS `reload` plugin → auto-reload do ~30s, bez restartu podu.

Watchout: TF teraz zmrazuje celý Corefile — upgrade ck-dns chartu môže meniť Corefile a TF/Helm
si budú prepisovať pole. Drž blok v sync s chartom.

`terraform validate` OK, targeted `terraform plan` = 1 to add. **NEaplikované** — čaká na user go
(shared-infra DNS + backend je dočasne `local`/teardown).

Status: open
Tags: #decision #coredns #dns #shared-infra #helm-ownership
